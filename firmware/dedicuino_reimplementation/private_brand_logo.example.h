#pragma once

#include <Arduino.h>

// Copy this file to `private_brand_logo.h` and replace with your private logo bitmap.
// This file is safe to commit. The copied `private_brand_logo.h` is gitignored.

static const uint8_t PRIVATE_BRAND_LOGO_WIDTH = 1;
static const uint8_t PRIVATE_BRAND_LOGO_HEIGHT = 1;

static const unsigned char PRIVATE_BRAND_LOGO_BITMAP[] PROGMEM = {
  0x00
};
